# Core Discord bot
discord.py==2.4.0
python-dotenv==1.0.1

# New LLM stack (Phase 1)
google-genai>=0.2.0
yfinance>=0.2.43

# Trading SDK (Phase 2 - E.SUN Trade SDK)
# E.SUN Trade SDK must be installed manually from wheel file
# Download from: https://www.esunsec.com.tw/trading-platforms/api-trading/docs/download/download-sdk/
# Install with: pip install wheels/esun_trade-2.2.0-*.whl
# esun-trade==2.2.0 (manual installation required)

# Testing
pytest==8.3.2
pytest-asyncio==0.23.8
pytest-mock==3.14.0

# Utilities (keep if needed by other parts)
keyring==23.5.0
keyrings.cryptfile==1.3.8
